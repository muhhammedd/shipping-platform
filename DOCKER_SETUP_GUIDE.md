# 🐳 دليل تشغيل المشروع باستخدام Docker

هذا الدليل سيساعدك على تشغيل منصة الشحن بالكامل على جهازك المحلي باستخدام Docker و Docker Compose.

## 🛠️ المتطلبات المسبقة
- تثبيت **Docker Desktop** على جهازك.
- التأكد من أن المنافذ التالية غير مستخدمة: `3000`, `3001`, `3002`, `3003`, `5432`, `8080`.

## 🚀 خطوات التشغيل لأول مرة

### 1. تجهيز ملفات البيئة
قم بإنشاء ملف باسم `.env` في المجلد الرئيسي للمشروع وانسخ المحتويات من ملف `.env.docker` الذي قمت بإنشائه لك.

### 2. بناء وتشغيل الحاويات (Containers)
افتح Terminal في المجلد الرئيسي للمشروع وقم بتشغيل الأمر التالي:
```bash
docker-compose up -d --build
```
هذا الأمر سيقوم ببناء الصور (Images) لكل من:
- **API Server**: على المنفذ `3001`
- **Admin Dashboard**: على المنفذ `3000`
- **Merchant Dashboard**: على المنفذ `3002`
- **Courier PWA**: على المنفذ `3003`
- **PostgreSQL**: قاعدة البيانات
- **Redis**: للتخزين المؤقت
- **pgAdmin**: لإدارة قاعدة البيانات على المنفذ `8080`

### 3. تهيئة قاعدة البيانات (Migrations & Seeding)
بعد التأكد من أن جميع الحاويات تعمل، نحتاج لإنشاء الجداول وإضافة البيانات التجريبية:
```bash
# إنشاء الجداول
docker exec -it shipping-api npx prisma migrate deploy

# إضافة البيانات التجريبية (اختياري)
docker exec -it shipping-api npm run db:seed
```

## 📊 الوصول إلى الخدمات

| الخدمة | الرابط | ملاحظات |
| :--- | :--- | :--- |
| **Admin Dashboard** | [http://localhost:3000](http://localhost:3000) | لوحة تحكم الإدارة |
| **API Server** | [http://localhost:3001/v1](http://localhost:3001/v1) | الخادم الخلفي |
| **Merchant Dashboard** | [http://localhost:3002](http://localhost:3002) | لوحة تحكم التاجر |
| **Courier PWA** | [http://localhost:3003](http://localhost:3003) | تطبيق المندوب |
| **pgAdmin 4** | [http://localhost:8080](http://localhost:8080) | البريد: `admin@shipping.com` / كلمة السر: `admin` |

## 🛠️ إدارة قاعدة البيانات عبر pgAdmin
1. افتح [http://localhost:8080](http://localhost:8080).
2. قم بتسجيل الدخول.
3. أضف خادم جديد (Add New Server):
   - **Name**: `Shipping Platform`
   - **Connection Tab**:
     - **Host**: `postgres` (اسم الحاوية داخل Docker)
     - **Port**: `5432`
     - **Username**: `shipping`
     - **Password**: `shipping_secret`

## 🛑 إيقاف المشروع
لإيقاف جميع الخدمات:
```bash
docker-compose down
```
