import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Note: optimizePackageImports removed — behavior differs between Next.js 14 and 15
};

export default nextConfig;
